//zadaca 1

var cur_age = 23; // 67 * 10 * 365
var max_age = 90;
var max_eat = 10;
console.log(`You will need ${(max_age - cur_age) * max_eat * 365} to last you until the ripe old age of ${max_age}.`);

// zadaca 2

var  r = 10;
console.log(`The circumference is ${r * 2}.`);
console.log(`The area is ${Math.pow(r, 2) * Math.PI}.`);

//zadaca 3

var cel = 20;
console.log(`${cel}°C is ${cel / 5 * 9 + 32}°F.`);
var fah = 68;
console.log(`${fah}°F is ${(fah - 32) * 5 / 9}°C.`);
